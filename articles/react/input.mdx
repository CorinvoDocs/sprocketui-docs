---
title: Input
description: A flexible text input component with built-in validation, states, and accessibility features.
category: Form
links:
  github: https://github.com/sprocketui/NodeKit/tree/main/packages/@sprocketui-react/sprocket-react-input
  npm: https://www.npmjs.com/package/@sprocketui-react/input
  bundleSize: https://bundlephobia.com/package/@sprocketui-react/input
  ariaPattern: https://www.w3.org/WAI/ARIA/apg/patterns/textbox/
features:
  - Native input validation with custom error messages.
  - Supports controlled and uncontrolled modes.
  - Polymorphic component rendering via Necto <Primitive />.
  - Focus visible styles for keyboard navigation.
  - Automatic aria-invalid handling for validation states.
---

<Install packageName="@sprocket-ui/input" />

<Preview
  component="input"
  filename="input"
  language="tsx"
  code={'import { Input } from \'@sprocket-ui/input\';\n\nfunction Example() {\n  return (\n    <Input\n
placeholder="Enter your email"\n      type="email"\n      className="px-4 py-2 border rounded-lg w-full"\n    />\n  );\n}'}
/>

## Usage

The Input component is a flexible, accessible text input with support for various input types and validation states.

### Basic Example

```tsx
import { Input } from '@sprocket-ui/input';

<Input
  placeholder="Enter text..."
  onChange={(e) => console.log(e.target.value)}
/>
```

Input Types

{/* Text input */}
<Input type="text" placeholder="Username" />

{/* Email input */}
<Input type="email" placeholder="email@example.com" />

{/* Password input */}
<Input type="password" placeholder="Password" />

{/* Number input */}
<Input type="number" placeholder="0" min={0} max={100} />

{/* Search input */}
<Input type="search" placeholder="Search..." />

{/* URL input */}
<Input type="url" placeholder="https://example.com" />

{/* Tel input */}
<Input type="tel" placeholder="+1 (555) 000-0000" />

Controlled Input

import { useState } from 'react';
import { Input } from '@sprocket-ui/input';

function ControlledExample() {
  const [value, setValue] = useState('');

  return (
    <Input
      value={value}
      onChange={(e) => setValue(e.target.value)}
      placeholder="Controlled input"
    />
  );
}

States

{/* Disabled state */}
<Input isDisabled placeholder="Disabled input" />

{/* Read-only state */}
<Input isReadOnly value="Read-only value" />

{/* Invalid state */}
<Input isInvalid placeholder="Invalid input" />

{/* Required field */}
<Input isRequired placeholder="Required field" />

With Validation

import { useState } from 'react';
import { Input } from '@sprocket-ui/input';

function ValidationExample() {
  const [email, setEmail] = useState('');
  const [isInvalid, setIsInvalid] = useState(false);

  const validateEmail = (value: string) => {
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    setIsInvalid(!emailRegex.test(value) && value.length > 0);
  };

  return (
    <div className="flex flex-col gap-1">
      <Input
        type="email"
        value={email}
        onChange={(e) => {
          setEmail(e.target.value);
          validateEmail(e.target.value);
        }}
        isInvalid={isInvalid}
        placeholder="Enter email"
      />
      {isInvalid && (
        <span className="text-sm text-red-500">
          Please enter a valid email address
        </span>
      )}
    </div>
  );
}

With Label

import { Input } from '@sprocket-ui/input';
import { Label } from '@sprocket-ui/label';

<div className="flex flex-col gap-2">
  <Label htmlFor="username">Username</Label>
  <Input id="username" placeholder="Enter username" />
</div>

<API
  component="input"
  framework="react"
  sections={[
    {
      title: 'Props',
      props: [
        {
          name: 'type',
          type: "'text' | 'email' | 'password' | 'number' | 'search' | 'url' | 'tel'",
          default: "'text'",
          description: 'The type of input to render.',
        },
        {
          name: 'value',
          type: 'string',
          default: '—',
          description: 'The current value of the input (controlled mode).',
        },
        {
          name: 'defaultValue',
          type: 'string',
          default: '—',
          description: 'The default value of the input (uncontrolled mode).',
        },
        {
          name: 'placeholder',
          type: 'string',
          default: '—',
          description: 'Placeholder text shown when the input is empty.',
        },
        {
          name: 'isDisabled',
          type: 'boolean',
          default: 'false',
          description: 'Whether the input is disabled and cannot be interacted with.',
        },
        {
          name: 'isReadOnly',
          type: 'boolean',
          default: 'false',
          description: 'Whether the input is read-only and cannot be modified.',
        },
        {
          name: 'isRequired',
          type: 'boolean',
          default: 'false',
          description: 'Whether the input is required in a form context.',
        },
        {
          name: 'isInvalid',
          type: 'boolean',
          default: 'false',
          description: 'Whether the input is in an invalid state.',
        },
        {
          name: 'autoFocus',
          type: 'boolean',
          default: 'false',
          description: 'Whether the input should receive focus on mount.',
        },
        {
          name: 'autoComplete',
          type: 'string',
          default: '—',
          description: 'Hint for form autofill feature.',
        },
        {
          name: 'maxLength',
          type: 'number',
          default: '—',
          description: 'Maximum number of characters allowed.',
        },
        {
          name: 'minLength',
          type: 'number',
          default: '—',
          description: 'Minimum number of characters required.',
        },
        {
          name: 'pattern',
          type: 'string',
          default: '—',
          description: 'Regular expression pattern for validation.',
        },
        {
          name: 'name',
          type: 'string',
          default: '—',
          description: 'Name of the input for form submission.',
        },
        {
          name: 'className',
          type: 'string',
          default: '—',
          description: 'Additional CSS classes to apply to the input.',
        },
        {
          name: 'style',
          type: 'CSSProperties',
          default: '—',
          description: 'Inline styles to apply to the input.',
        },
      ],
    },
    {
      title: 'Number Input Props',
      props: [
        {
          name: 'min',
          type: 'number',
          default: '—',
          description: 'Minimum value for number inputs.',
        },
        {
          name: 'max',
          type: 'number',
          default: '—',
          description: 'Maximum value for number inputs.',
        },
        {
          name: 'step',
          type: 'number',
          default: '—',
          description: 'Step increment for number inputs.',
        },
      ],
    },
    {
      title: 'Events',
      props: [
        {
          name: 'onChange',
          type: '(e: ChangeEvent) => void',
          default: '—',
          description: 'Handler that is called when the input value changes.',
        },
        {
          name: 'onFocus',
          type: '(e: FocusEvent) => void',
          default: '—',
          description: 'Handler that is called when the input receives focus.',
        },
        {
          name: 'onBlur',
          type: '(e: FocusEvent) => void',
          default: '—',
          description: 'Handler that is called when the input loses focus.',
        },
        {
          name: 'onFocusChange',
          type: '(isFocused: boolean) => void',
          default: '—',
          description: 'Handler that is called when the focus state changes.',
        },
        {
          name: 'onKeyDown',
          type: '(e: KeyboardEvent) => void',
          default: '—',
          description: 'Handler that is called when a key is pressed.',
        },
        {
          name: 'onKeyUp',
          type: '(e: KeyboardEvent) => void',
          default: '—',
          description: 'Handler that is called when a key is released.',
        },
        {
          name: 'onInput',
          type: '(e: FormEvent) => void',
          default: '—',
          description: 'Handler that is called when the input receives input.',
        },
        {
          name: 'onSelect',
          type: '(e: SyntheticEvent) => void',
          default: '—',
          description: 'Handler that is called when text is selected in the input.',
        },
        {
          name: 'onCopy',
          type: '(e: ClipboardEvent) => void',
          default: '—',
          description: 'Handler that is called when content is copied.',
        },
        {
          name: 'onCut',
          type: '(e: ClipboardEvent) => void',
          default: '—',
          description: 'Handler that is called when content is cut.',
        },
        {
          name: 'onPaste',
          type: '(e: ClipboardEvent) => void',
          default: '—',
          description: 'Handler that is called when content is pasted.',
        },
      ],
    },
    {
      title: 'Accessibility',
      props: [
        {
          name: 'id',
          type: 'string',
          default: '—',
          description: 'The elements unique identifier. Used for label association.',
        },
        {
          name: 'aria-label',
          type: 'string',
          default: '—',
          description: 'Defines a string value that labels the input.',
        },
        {
          name: 'aria-labelledby',
          type: 'string',
          default: '—',
          description: 'Identifies the element(s) that label the input.',
        },
        {
          name: 'aria-describedby',
          type: 'string',
          default: '—',
          description: 'Identifies the element(s) that describe the input.',
        },
        {
          name: 'aria-details',
          type: 'string',
          default: '—',
          description: 'Identifies the element(s) that provide detailed description.',
        },
        {
          name: 'aria-invalid',
          type: 'boolean',
          default: '—',
          description: 'Indicates the entered value does not conform to the expected format. Automatically set when isInvalid
is true.',
        },
        {
          name: 'aria-errormessage',
          type: 'string',
          default: '—',
          description: 'Identifies the element that provides an error message for the input.',
        },
      ],
    },
  ]}
/>

Accessibility

The Input component follows WAI-ARIA best practices:

- Uses semantic <input> element for proper form integration
- Automatically sets aria-invalid when isInvalid prop is true
- Automatically sets aria-disabled when isDisabled prop is true
- Automatically sets aria-readonly when isReadOnly prop is true
- Automatically sets aria-required when isRequired prop is true
- Supports all ARIA attributes for enhanced accessibility
- Focus management and keyboard navigation work as expected
- Screen readers properly announce validation states

Styling

The Input component is unstyled by default and supports full customization through the className prop:

{/* Basic styling */}
<Input
  className="px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
  placeholder="Styled input"
/>

{/* Invalid state styling */}
<Input
  isInvalid
  className="px-4 py-2 border border-red-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500"
  placeholder="Invalid input"
/>

{/* Disabled styling */}
<Input
  isDisabled
  className="px-4 py-2 border border-gray-200 rounded-lg bg-gray-100 text-gray-400 cursor-not-allowed"
  placeholder="Disabled input"
/>
