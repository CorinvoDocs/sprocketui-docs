---
title: Button
description: A customizable button component with multiple variants and states.
category: Form
links:
  github: https://github.com/sprocket-ui/NodeKit/tree/main/packages/%40sprocketui-react/sprocketui-react-button
  npm: https://www.npmjs.com/package/@sprocketui-react/button
  bundleSize: https://bundlephobia.com/package/@sprocketui-react/button
  ariaPattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/
features:
    - Supports keyboard activation with Enter and Space keys.
    - Automatic aria-disabled handling when disabled.
    - Polymorphic component rendering via Necto <Primitive />.
    - Focus visible styles for keyboard navigation.
    - Loading state with automatic disabled behavior.
---

<Preview
  storyId="components-button--default"
  code={`
    import { Button } from '@sprocketui/react';

    const ButtonDemo = () => {
      return (
        <Button>INITIALIZE</Button>
      )
    }

    export default ButtonDemo;
  `}
/>

## Installation

<Install package="@sprocketui-react/button" />

### Anatomy

Import from collection package and use `Root` component.

```tsx 
import { Button } from "@sprocketui/react";

export default () => (
  <Button.Root>
    Click Me
  </Button.Root>
);
```

You can also import pre-defined aliases for when there are namespace issues.

```tsx 
import { SprocketButton } from "@sprocketui/react";

export default () => (
  <SprocketButton.Root>
    Click Me
  </SprocketButton.Root>
);
```

### States

```tsx
{/* Loading state */}
<Button isPending>Loading...</Button>

{/* Disabled state */}
<Button isDisabled>Disabled</Button>
```

## API Reference

### Root

Contains the content for the button.

<API
  component="button"
  framework="react"
  sections={[
    {
      title: 'Props',
      props: [
        {
          name: 'isPending',
          type: 'boolean',
          default: '—',
          description: 'Whether the button is in a pending state. This disables press and hover events while retaining focusability, and announces the pending state to screen readers.',
        },
        {
          name: 'isDisabled',
          type: 'boolean',
          default: '—',
          description: 'Whether the button is disabled. This prevents the user from interacting with the button.',
        },
        {
          name: 'autoFocus',
          type: 'boolean',
          default: '—',
          description: 'Whether the element should receive focus on render.',
        },
        {
          name: 'children',
          type: 'ReactNode',
          default: '—',
          description: 'The content to display inside the button.',
        },
        {
          name: 'className',
          type: 'string',
          default: '—',
          description: 'Additional CSS classes to apply to the button.',
        },
        {
          name: 'style',
          type: 'CSSProperties',
          default: '—',
          description: 'Inline styles to apply to the button.',
        },
      ],
    },
    {
      title: 'Events',
      props: [
        {
          name: 'onPress',
          type: '(e: PressEvent) => void',
          default: '—',
          description: 'Handler that is called when the press is released over the target.',
        },
        {
          name: 'onPressStart',
          type: '(e: PressEvent) => void',
          default: '—',
          description: 'Handler that is called when a press interaction starts.',
        },
        {
          name: 'onPressEnd',
          type: '(e: PressEvent) => void',
          default: '—',
          description: 'Handler that is called when a press interaction ends, either over the target or when the pointer leaves the target.',
        },
        {
          name: 'onPressChange',
          type: '(isPressed: boolean) => void',
          default: '—',
          description: 'Handler that is called when the press state changes.',
        },
        {
          name: 'onPressUp',
          type: '(e: PressEvent) => void',
          default: '—',
          description: 'Handler that is called when a press is released over the target, regardless of whether it started on the target or not.',
        },
        {
          name: 'onFocus',
          type: '(e: FocusEvent) => void',
          default: '—',
          description: 'Handler that is called when the element receives focus.',
        },
        {
          name: 'onBlur',
          type: '(e: FocusEvent) => void',
          default: '—',
          description: 'Handler that is called when the element loses focus.',
        },
        {
          name: 'onFocusChange',
          type: '(isFocused: boolean) => void',
          default: '—',
          description: 'Handler that is called when the element\'s focus status changes.',
        },
        {
          name: 'onKeyDown',
          type: '(e: KeyboardEvent) => void',
          default: '—',
          description: 'Handler that is called when a key is pressed.',
        },
        {
          name: 'onKeyUp',
          type: '(e: KeyboardEvent) => void',
          default: '—',
          description: 'Handler that is called when a key is released.',
        },
      ],
    },
    {
      title: 'Layout',
      props: [
        {
          name: 'slot',
          type: 'string',
          default: '—',
          description: 'A slot name for the component. Slots allow the component to receive props from a parent component.',
        },
      ],
    },
    {
      title: 'Accessibility',
      props: [
        {
          name: 'id',
          type: 'string',
          default: '—',
          description: 'The element\'s unique identifier.',
        },
        {
          name: 'excludeFromTabOrder',
          type: 'boolean',
          default: '—',
          description: 'Whether to exclude the element from the sequential tab order. If true, the element will not be focusable via the keyboard by tabbing.',
        },
        {
          name: 'aria-label',
          type: 'string',
          default: '—',
          description: 'Defines a string value that labels the current element.',
        },
        {
          name: 'aria-labelledby',
          type: 'string',
          default: '—',
          description: 'Identifies the element (or elements) that labels the current element.',
        },
        {
          name: 'aria-describedby',
          type: 'string',
          default: '—',
          description: 'Identifies the element (or elements) that describes the object.',
        },
        {
          name: 'aria-details',
          type: 'string',
          default: '—',
          description: 'Identifies the element (or elements) that provide a detailed, extended description for the object.',
        },
        {
          name: 'aria-controls',
          type: 'string',
          default: '—',
          description: 'Identifies the element (or elements) whose contents or presence are controlled by the current element.',
        },
        {
          name: 'aria-expanded',
          type: 'boolean | "true" | "false"',
          default: '—',
          description: 'Indicates whether the element, or another grouping element it controls, is currently expanded or collapsed.',
        },
        {
          name: 'aria-haspopup',
          type: 'boolean | "menu" | "listbox" | "tree" | "grid" | "dialog"',
          default: '—',
          description: 'Indicates the availability and type of interactive popup element, such as menu or dialog, that can be triggered by an element.',
        },
        {
          name: 'aria-pressed',
          type: 'boolean | "true" | "false" | "mixed"',
          default: '—',
          description: 'Indicates the current "pressed" state of toggle buttons.',
        },
      ],
    },
  ]}
/>

## Accessibility

This component is based on the native ⁠`button` element and supports all of its props. It handles press interactions across mouse, touch, pen, and keyboard, normalizing behavior across browsers and platforms. It includes support for pressed states, disabled states, and accessible labelling via ARIA attributes.

## Styling

The Button component is built with Tailwind CSS and supports full customization through the `className` prop. You can extend or override any of the default styles.
